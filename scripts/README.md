# Scripts

Optional helpers; the repo works fine with only `go` and `make`.

| Script | Purpose |
|--------|---------|
| `build.ps1` | Windows PowerShell: `go build` into `tunnelbypass.exe` |

Add your own CI wrappers here (e.g. sign binaries, upload artifacts). Keep secrets out of the repo.
