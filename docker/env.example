# Copy to docker/.env (gitignored). Reference: env_file: - docker/.env

# TB_DATA_DIR=/data
# TB_PORTABLE=1
# TB_LOG=json
# TB_SSH_LISTEN=2222
# TB_UDPGW_PORT=7300
# TB_SSH_PASSWORD=your-secret
# TB_LOGS_DIR=/data/logs
# TB_DEP_START_TIMEOUT_MS=120000
# TB_ALLOW_SVC_IN_CONTAINER=1   # Rare: allow wizard/service CreateService inside Docker (default: off)
